import { ITaskControls } from '@/types/interfaces'

export const taskControlsDict: Array<ITaskControls> = [
    { id: 1, act: () => {}, variant: 'image-like' },
    { id: 2, act: () => {}, variant: 'image-like' },
]
